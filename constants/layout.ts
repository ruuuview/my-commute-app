// constants/layout.ts

export const LINE_CARD_HEIGHT = 72;    // px — enforced, never variable
export const STATION_CARD_HEIGHT = 68; // px — enforced, never variable
export const CARD_HORIZONTAL_MARGIN = 16;
export const CARD_VERTICAL_GAP = 10;
