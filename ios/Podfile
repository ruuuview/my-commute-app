require File.join(File.dirname(`node --print "require.resolve('expo/package.json')"`), "scripts/autolinking")
require File.join(File.dirname(`node --print "require.resolve('react-native/package.json')"`), "scripts/react_native_pods")
require 'fileutils'
require 'json'

podfile_properties = JSON.parse(File.read(File.join(__dir__, 'Podfile.properties.json'))) rescue {}
platform :ios, podfile_properties['ios.deploymentTarget'] || '15.1'
prepare_react_native_project!

target 'MyCommute' do
  use_expo_modules!
  config = use_native_modules!

  # --------------------------------------------------------
  # 🔒 THE VERSION LOCK
  # We align ALL versions to a matching set from April 2024.
  # This set is known to be stable and devoid of the -G flag issue.
  # --------------------------------------------------------
  
  # 1. The Safe gRPC (No -G Flag)
  pod 'gRPC-Core', '1.62.2', :modular_headers => true
  pod 'gRPC-C++', '1.62.2', :modular_headers => true
  pod 'BoringSSL-GRPC', '0.0.32', :modular_headers => true

  # 2. The Matching Firebase Set (Compatible with gRPC 1.62.2)
  # We override the versions react-native-firebase tries to pick
  $firebase_version = '10.24.0'
  pod 'Firebase/Auth', $firebase_version
  pod 'Firebase/Firestore', $firebase_version
  pod 'FirebaseCore', $firebase_version
  pod 'FirebaseCoreInternal', $firebase_version
  pod 'FirebaseCoreExtension', $firebase_version
  pod 'FirebaseFirestoreInternal', $firebase_version
  pod 'GoogleUtilities', :modular_headers => true

  use_react_native!(
    :path => config[:reactNativePath],
    :hermes_enabled => true,
    :app_path => "#{Pod::Config.instance.installation_root}/..",
    :privacy_file_aggregation_enabled => true,
  )

  post_install do |installer|
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false
    )

    puts "🔧 RUNNING FINAL SAFETY CHECKS..."

    # 1. INJECTION FIX
    rn_root = File.expand_path("../node_modules/react-native", __dir__)
    source_file = Dir.glob("#{rn_root}/**/RCTTurboModule.h").first
    if source_file
      dest_dir = File.join(installer.sandbox.root, "Headers/Public/ReactCommon")
      FileUtils.mkdir_p(dest_dir)
      FileUtils.cp(source_file, dest_dir)
    end

    # 2. C++ PATCHES
    header_files = Dir.glob("Pods/**/*.{h,hpp}")
    header_files.each do |file_path|
      begin
        content = File.read(file_path)
        original_content = content.dup
        content.gsub!(/std::result_of/, 'std::invoke_result')
        content.gsub!(/std::invoke_result<F\(\)>/, 'std::invoke_result<F>')
        content.gsub!(/Traits::template CallSeqFactory\(/, 'Traits::template CallSeqFactory<>(')
        if content != original_content
          File.write(file_path, content)
        end
      rescue; end
    end
    
    # 3. SAFETY NET: Scrub BoringSSL assembly just in case
    # Even though we pinned versions, this ensures no stray flags survive
    Dir.glob("Pods/BoringSSL-GRPC/**/*.{S,s}").each do |asm_file|
      begin
        content = File.read(asm_file)
        if content.include?('-G')
           content.gsub!(/ -G /, ' ')
           content.gsub!(/-G /, '')
           content.gsub!(/ -G$/, '')
           File.write(asm_file, content)
           puts "   ✅ Patched assembly file: #{File.basename(asm_file)}"
        end
      rescue; end
    end

    # 4. STANDARD BUILD CONFIGURATIONS
    installer.pods_project.targets.each do |target|
      target.build_configurations.each do |config|
        config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.1'
        config.build_settings['CLANG_CXX_LANGUAGE_STANDARD'] = 'c++20'
        config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] ||= ['$(inherited)']
        config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] << '_LIBCPP_ENABLE_CXX20_REMOVED_TYPE_TRAITS=1'
        config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'
        config.build_settings['GCC_TREAT_WARNINGS_AS_ERRORS'] = 'NO'
      end
    end
    
    puts "✅ VERSIONS ALIGNED & PATCHED"
  end
end

target 'CommuteWidgetExtension' do
  # Keep empty
end